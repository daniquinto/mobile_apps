# docs

| File | Description |
|------|-------------|
| `data-model.md` | Full explanation of SQL normalization decisions and MongoDB design choices. |
| `postman_collection.json` | Import this file into Postman to test all endpoints immediately. |

To import the Postman collection: open Postman → Import → select `postman_collection.json`.
Set the `base` variable to `http://localhost:3000/api`.
